/** Layout barrel: pure, deterministic geometry assignment (no logic). */

export { layoutCircuit, LAYOUT_METRICS, naturalCompare, portAnchor } from './engine.ts';
export type {
  Box,
  LayoutResult,
  Orientation,
  PlacedComponent,
  Point,
  RoutedConnection,
} from './types.ts';
