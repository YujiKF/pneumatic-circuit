{
  "name": "pmr3407-pneumatic-circuit-generator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Deterministic generator of pneumatic and electropneumatic circuits based on the methods taught in PMR3407 (Sistemas Fluido-Mecanicos / Pneumatica, EESP-USP).",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node --test \"tests/**/*.test.ts\" \"src/**/*.test.ts\"",
    "typecheck:ui": "tsc --noEmit -p src/ui/tsconfig.json",
    "dev": "vite",
    "build:ui": "vite build",
    "preview": "vite preview"
  },
  "//dependencies_note": "The CORE has ZERO npm dependencies. The dependencies below are ONLY for the best-effort React+Vite UI and CANNOT be installed in this sandbox (npm registry blocked, HTTP 403). Run `npm install` in a networked environment to enable the UI; the core typecheck/test never needs them.",
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.5",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.0"
  }
}
